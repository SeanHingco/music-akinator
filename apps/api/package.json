{
  "name": "@music-akinator/api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "./.venv/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "./.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "build": "echo \"(python: no build step)\"",
    "lint": "echo \"(add ruff/mypy later)\"",
    "test": "echo \"(add pytest later)\""
  }
}
